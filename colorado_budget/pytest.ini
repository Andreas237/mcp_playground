[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that make real network calls (no LLM)
    slow: tests that invoke the LLM (~30-120s each)
addopts = -ra --tb=short
